<nav>
    {% if app.user %}

        {% if app.user.administrateur%}
            <ul>
            <li><a href="#" title="Villes">Villes</a></li>
            <li><a href="#" title="Campus">Campus</a></li>

        {% endif %}
        <li><a href="{{ path('sortie_liste') }}" title="Accueil">Accueil</a></li>
        <li><a href="#" title="Mon Profil">Mon Profil</a></li>
        <li><a href="{{ path('app_logout') }}" title="Logout">Se déconnecter</a></li>
        </ul>
        {% endif %}

    </nav>